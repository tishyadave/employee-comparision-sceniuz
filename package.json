{
  "name": "employee-comparison-sceniuz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "seed": "npm run db:seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
